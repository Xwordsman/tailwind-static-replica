interface:
  display_name: "Tailwind Static Replica"
  short_description: "Recreate authorized websites as static Tailwind UI pages."
  default_prompt: "Use $tailwind-static-replica to recreate https://example.com as a static Tailwind site. Inspect the target structure first and decide the needed HTML pages and filenames."
